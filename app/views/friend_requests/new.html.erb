<h1>친구 요청을 보내는 페이지</h1>

<% if user_signed_in? %>
	<% for user in User.where.not(id: current_user.id) %>
	  <p>
	    <strong><%= user.nickname %></strong>
	    <%= link_to "친구요청 보내기", friend_requests_path(friend_id: user.id), :method => :post %>
	  </p>
	<% end %>
<% else %>
	로그인을 해주세요
<% end %>